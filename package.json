{
  "name": "try-sele",
  "version": "1.0.0",
  "description": "Selenium Webdriver tests using Mocha with Mochawesome reports",
  "scripts": {
    "test": "mocha tests/sesi10-test.js --recursive --timeout 60000 --reporter mochawesome"
  },
  "dependencies": {
    "selenium-webdriver": "4.34.0"
  },
  "devDependencies": {
    "mocha": "11.7.1",
    "mochawesome": "7.1.3",
    "mocha-simple-html-reporter": "2.0.0"
  }
}
